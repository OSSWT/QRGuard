# Published Structural dataset provenance

These manifests are the compact public audit records for the admitted external
Structural sources:

- `qrdn_manifest.csv` and `qrdn_preparation_audit.json`
- `qr_surfaces_manifest.csv` and `qr_surfaces_preparation_audit.json`
- `download_verification.json`

They are retained because the canonical processed images are intentionally
Git-ignored. Obsolete combined candidate manifests are not copied here; current
r07 dataset identity and retention policy are recorded under `ml_training/`.
