# Structural notebooks

`structural_training_v3.ipynb` is the canonical Colab notebook for
`structural-2026.09-r07`. Earlier candidate notebooks are retained outside the public
repository under the local ML history archive.

Run `python scripts/build_colab_bundle.py` to sync this notebook and create the
ignored upload bundle under `dist/`.
